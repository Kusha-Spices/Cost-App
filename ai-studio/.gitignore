node_modules/
dist/
*.log
.DS_Store

# The repo-root .gitignore excludes build/ (PyInstaller output of the costing
# app). For this Electron app, build/ holds the app icon + generator — keep them.
!build/
!build/**
