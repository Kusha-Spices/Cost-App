streamlit>=1.36.0
openpyxl>=3.1.2
pandas>=2.2.0
# Desktop app: native window around the Streamlit UI (falls back to the browser).
pywebview>=5.0
# Windows native-window backend (pywebview uses .NET WinForms/WebView2 via pythonnet).
pythonnet>=3.0.3; sys_platform == "win32"
